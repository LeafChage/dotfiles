# Global instructions

Before substantive work in any project, read
`/Users/coffeebeagle/Documents/CoffeeBeagle/_claude-memory/README.md` and
the files it links to. These notes describe how Kusanagi thinks, works,
and prefers to collaborate — apply them regardless of which project this
session is in. They are ordinary local files; read and edit them directly,
not through the `github` MCP tools.

When a genuinely new, recurring, or corrected pattern about how Kusanagi
works with Claude shows up in *any* project, update the relevant file
there and commit it with a message saying what changed and why. Notes must
be in English regardless of the conversation's language.

That folder is a git clone of `LeafChage/claude-memory`, synced by the
obsidian-git plugin. The vault is the master; the repo is a mirror that
exists for mobile access. Auto-commit is off — commit there yourself
(including anything already pending from Kusanagi's own edits, which
nothing else commits), but never push by hand. Moved there on 2026-08-05 — see
`_claude-memory/key-decisions/2026-08-05-memory-back-to-obsidian.md`. It
lived in that repo between 2026-07-19 and 2026-08-05, and in the vault's
`_claude-notes/` (now deleted) before that; ignore any pointer to either.
